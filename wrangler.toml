name = "fold"
compatibility_date = "2024-12-01"

# ADMIN_PASSWORD is read from a Cloudflare Pages secret (encrypted variable),
# NOT stored here. Set it in the Pages dashboard: Settings > Environment variables.
# Used by /api/admin/login to issue session tokens.

[[d1_databases]]
binding = "DB"
database_name = "fold"
database_id = "3a51c742-8053-43c6-b6de-b717843085e5"
